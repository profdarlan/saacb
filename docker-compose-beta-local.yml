version: '3.8'

name: saacb-beta

services:
  saacb-beta:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: saacb-django-beta
    command: gunicorn projeto_saacb.wsgi:application --bind 0.0.0.0:8000 --workers 2 --worker-class sync --timeout 120 --keep-alive 5 --log-level info
    ports:
      - "30011:8000"
    environment:
      - DEBUG=True
      - SECRET_KEY=django-insecure-saacb-beta
      - ALLOWED_HOSTS=localhost,127.0.0.1,192.168.1.51
      - DATABASE_URL=sqlite:////app/data/db.sqlite3
      - SISGRU_USUARIO=
      - SISGRU_SENHA=
      - SISGRU_PRODUCAO=False
      - CALCULADORA_API_URL=http://planilha-calculos:8000
      - CALCULADORA_API_TOKEN=
      - OLLAMA_HOST=http://host.docker.internal:11434
      - OLLAMA_MODEL=llama3:8b
      - SECURE_SSL_REDIRECT=False
    volumes:
      - ./data:/app/data
      - ./media:/app/media
      - ./static:/app/static
    depends_on:
      - planilha-calculos
    restart: unless-stopped
    networks:
      - saacb-beta-network

  planilha-calculos-beta:
    build:
      context: ../planilha_saacb
      dockerfile: Dockerfile
    container_name: planilha-calculos-beta
    ports:
      - "8003:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - ../planilha_saacb:/app
      - planilha-beta-data:/app/data
    restart: unless-stopped
    networks:
      - saacb-beta-network

volumes:
  planilha-beta-data:

networks:
  saacb-beta-network:
    driver: bridge
